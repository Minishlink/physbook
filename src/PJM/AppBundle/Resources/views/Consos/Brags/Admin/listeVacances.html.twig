<div class="panel panel-default">
    <div class="panel-heading" role="tab" id="headingVacances" data-toggle="collapse" data-parent="#accordion" data-target="#collapseVacances" aria-expanded="true" aria-controls="collapseVacances">
        <h2 class="panel-title">
                Gestion vacances/jours fériés
        </h2>
    </div>
    <div id="collapseVacances" class="panel-collapse collapse" role="tabpanel" aria-labelledby="headingVacances">
        <div class="panel-body">
            <p>
                Ce module sert à définir les vacances et jours fériés de façon à ce que les utilisateurs ne soient pas débités quand il n'y a pas de pain.
            </p>
            <h3>Ajout</h3>
            {{ form(form) }}

            <h3>Liste</h3>
            {{ datatable_render(datatable) }}
            {% if listeVacances != null %}
            <div class="table-responsive">
                <table id="tabGestionVacances" class="table table-striped table-hover table-condensed">
                    <thead>
                        <tr>
                            <th>Début</th>
                            <th>Fin</th>
                            <th>Jours</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for vacances in listeVacances %}
                        <tr>
                            <td>{{ vacances.dateDebut|date('d/m/y') }}</td>
                            <td>{{ vacances.dateFin|date('d/m/y') }}</td>
                            <td>{{ vacances.getNbJours() }}</td>
                            <td>{% if not vacances.fait %}<a href="{{ path('pjm_app_consos_brags_admin_annulerVacances', {'vacances': vacances.id}) }}">Supprimer</a>{% endif %}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
            {% else %}
            Il n'y a pas de vacances/jours fériés enregistrés.
            {% endif %}
        </div>
    </div>
</div>
