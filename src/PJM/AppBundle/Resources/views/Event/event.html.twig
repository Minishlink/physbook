{% if event is not null %}
<h2>{{ event.nom }}</h2>

<p>
créé par {% render(url('pjm_profil_encart', {'username': event.createur.username})) %} le {{ event.dateDebut|date('d/m/Y') }}.
</p>
{% endif %}
