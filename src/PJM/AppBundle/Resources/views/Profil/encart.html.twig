{% spaceless %}
<a
  href="{{ path('pjm_profil_voir', {'username': user.username}) }}"
  class="show_user" data-url="{{ path('pjm_profil_encart', {'username': user.username, 'content': true}) }}"
  title="Profil de {{ user.bucque }}"
>
{% if citation is defined %}@{{ user.username }}{% else %}{% if long is defined %}{{ user }}{% else %}{{ user.bucque }}{% endif %}{% endif %}
</a>
{% endspaceless %}
