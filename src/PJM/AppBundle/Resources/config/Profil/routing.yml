pjm_profil_modifier:
    path:       /modifier
    defaults:   { _controller: PJMAppBundle:Profil:modifier }

fos_user_profile_show:
    path:       /
    defaults:   { _controller: PJMAppBundle:Profil:voir }

pjm_profil_voir:
    path:       /voir/{username}
    defaults:   { _controller: PJMAppBundle:Profil:voir }

pjm_profil_encart:
    path:       /encart/{username}
    defaults:   { _controller: PJMAppBundle:Profil:encart }
